import React, { useState } from 'react';
import './Settings.css';

function Settings() {
    
    return (
        <div className="container">
            <h1>Settings Dashboard</h1>
            <button className="add-button">Save Settings</button>
            <table>
               
            </table>
        </div>
    );
}

export default Settings;
